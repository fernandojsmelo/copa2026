# Carregado automaticamente pelo Vite em builds de produção (`vite build`,
# que é o que o Vercel roda) — não é segredo, vira parte do bundle JS público
# de qualquer forma, por isso pode ser versionado (diferente de .env/.env.local).
VITE_API_URL=https://backend-sigma-pearl-y56vgge5nd.vercel.app/api
